import Head from "next/head";
import "aos/dist/aos.css";
import Image from "next/image";
import Header from "../Components/header";
import Learn from "../Components/blockchain/learn";
import Homepage from "../Components/blockchain/home";
import Features from "../Components/blockchain/features";
import Concept from "../Components/blockchain/concept";
import Navbar from "../Components/navbar";
import Footer from "../Components/footer";
require('dotenv').config({ path: `${__dirname}/../.env` })
import { Appwrite } from "appwrite";

export default function Blockchain({blockChain}) {

  const {concept, features, home, learn} = blockChain.document[0];
  return (
    <div>
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <Header />
      <Navbar />
      <Homepage home={home} />
      <Concept concept={concept}/>
      <Features features={features} />
      <Learn learn={learn}/>
      <Footer />
    </div>
  );
}





const sdk = new Appwrite();

export const getStaticProps = async () => {
    sdk
      .setEndpoint("http://localhost/v1") // Your API Endpoint
      .setProject(process.env.PROJECT_ID); // Your project ID

    let promise = sdk.database.listDocuments(process.env.BLOCHAIN_DB);

    let blockChain = await promise;

  return {
    props: {
      blockChain
    },
  };
};
